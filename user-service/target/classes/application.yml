spring:
  application:
    name: user-service
  profiles:
    active: dev,prod

server:
  port: 8081  # Specify server port

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
